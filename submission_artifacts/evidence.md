# S6 evidence bundle

**9 of 9 requirements PASS.** Audit checks passed: 9/9.

Generated by `tds/evidence.py` from `reports/audit_report.json`, which re-derives every claim by reading the artifacts back off disk. Regenerate the whole tree with `python run_demo.py`.

Config: `config/run_config.json` sha256 `209ec6c1b314c1fb...`, seed `20260806`.

| Requirement | Result | Evidence | What was checked |
|---|---|---|---|
| Tokenizer integrity | **PASS** | Manifest record (`submission_artifacts/manifests/*.manifest.json`, `fixtures/tokenizer/tokenizer-sarvam1.json`) | tok_bb5115a36ddb across 33 manifests |
| Evaluation firewall | **PASS** | Blocked-shard event (`submission_artifacts/ledgers/firewall_report.json`) | 6 of 6208 blocked, 4 despite a trainable flag; 0 found in admitted shards |
| Packing correctness | **PASS** | Packed-batch report (`submission_artifacts/manifests/packing_report.json`, `submission_artifacts/ledgers/consumption.jsonl`) | 5 policies, context 192, position policy packed_reset_on_eos |
| Mixture compliance | **PASS** | Planned versus actual shares (`submission_artifacts/manifests/mixture_schedule.json`, `submission_artifacts/ledgers/opus_decisions.jsonl`) | 1170 decisions, max lane drift 0.0, 0 firewall hits rescued by a floor |
| OPUS audit trail | **PASS** | Candidate decision records (`submission_artifacts/manifests/mixture_schedule.json`, `submission_artifacts/ledgers/opus_decisions.jsonl`) | 1170 decisions, max lane drift 0.0, 0 firewall hits rescued by a floor |
| Crash recovery | **PASS** | Expected and resumed batch ids (`submission_artifacts/reports/resume_report.json`, `submission_artifacts/reports/fork_report.json`) | crash at step 100, rolled back to ckpt_00090, 10/10 steps identical |
| Replay | **PASS** | Original and replay hashes (`submission_artifacts/reports/replay_report.json`) | steps 40-60, 20/20 matched on 11 fields |
| Learning trace | **PASS** | Loss linked to source data (`submission_artifacts/ledgers/consumption.jsonl`, `submission_artifacts/ledgers/learning_ledger.json`) | 221 events, offsets contiguous=True, 10 shards with measured loss |
| Throughput | **PASS** | Performance report (`submission_artifacts/performance.json`) | 2968.8 useful tokens/sec, buckets reconcile=True |

## How to verify this yourself

```
python run_demo.py                    # regenerate everything
python -m tds.audit_cli               # re-run the audit against the artifacts
python -m pytest tests/ -q            # invariant tests
```

The audit imports no state from the training run. It opens `submission_artifacts/` and `work/shards/`, recomputes hashes from bytes, re-derives every admission verdict from its manifest fields, reconstructs the effective batch stream from the ledger, and recomputes the throughput arithmetic. A `PASS` above exists only where one of those recomputations agreed.
